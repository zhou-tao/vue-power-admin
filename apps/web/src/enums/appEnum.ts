export enum ThemeEnum {
  LIGHT = 'light',
  DARK = 'dark'
}

export enum CSSVarEnum {
  COLOR_PRIMARY = '--el-color-primary',
  COLOR_SUCCESS = '--el-color-success',
  CONTENT_HEIGHT = '--content-base-height',
  CONTENT_MIN_HEIGHT = '--content-min-height',
  CONTENT_NAX_HEIGHT = '--content-max-height'
}
